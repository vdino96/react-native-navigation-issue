/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 *
 * @format
 * @flow
 */

import React from 'react';
import {SafeAreaView, ScrollView, TouchableOpacity} from 'react-native';

const Fifth: () => React$Node = () => {
  return (
    <>
      <SafeAreaView>
        <ScrollView>
          <TouchableOpacity />
        </ScrollView>
      </SafeAreaView>
    </>
  );
};

export default Fifth;
